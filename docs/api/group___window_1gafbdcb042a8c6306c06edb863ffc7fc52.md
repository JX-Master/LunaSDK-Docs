# Luna::Window::get_monitor_supported_video_mode

```c++
VideoMode get_monitor_supported_video_mode(monitor_t monitor, u32 index)
```

Gets the supported video mode of the monitor at the specified index. 

