# Luna::Window::window_input_character_event_handler_t

```c++
using window_input_character_event_handler_t =  void(IWindow* window, c32 character)
```

The handler for window input character event. 

