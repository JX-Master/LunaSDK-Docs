# Luna::RHIError::device_reset

```c++
ErrCode device_reset()
```

The device failed due to a badly formed command. This is a run-time issue; The application should destroy and recreate the device. 

