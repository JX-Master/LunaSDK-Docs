# Luna::RHI::RasterizerDesc::slope_scaled_depth_bias

```c++
f32 slope_scaled_depth_bias
```

The slope-scaled depth bias value added to the depth value generated by the vertex shader in depth bias step. 

