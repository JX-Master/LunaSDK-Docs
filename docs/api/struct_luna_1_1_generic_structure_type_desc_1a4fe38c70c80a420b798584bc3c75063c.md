# Luna::GenericStructureTypeDesc::generic_parameter_names

```c++
Span<const Name> generic_parameter_names
```

## Overview
The names for every generic parameter. This is only used as hints for users. 

