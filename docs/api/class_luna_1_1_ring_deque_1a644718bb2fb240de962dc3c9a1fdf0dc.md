# Luna::RingDeque::empty

```c++
bool empty() const
```

Checks whether this queue is empty, that is, the size of this queue is `0`. 



## Return value
Returns `true` if this queue is empty, returns `false` otherwise. 

