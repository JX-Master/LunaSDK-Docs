# Luna::IReadWriteLock::acquire_write

```c++
virtual void acquire_write()=0
```

Acquires one write ownership of the lock. 

## Overview
This operation blocks the current thread until the ownership is acquired. 

