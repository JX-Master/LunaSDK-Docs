# Luna::MemoryLayoutMember::offset

```c++
usize offset
```

## Overview
The offset of this member in bytes. This property is filled by the memory layouting algorithm and can remain uninitialized when calculating memory layouts. 

