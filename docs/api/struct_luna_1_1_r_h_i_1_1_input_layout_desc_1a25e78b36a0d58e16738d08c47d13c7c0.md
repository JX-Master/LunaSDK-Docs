# Luna::RHI::InputLayoutDesc::bindings

```c++
Span<const InputBindingDesc> bindings
```

An array of vertex input binding descriptors, each of them describes one vertex buffer that will be bound to the pipeline. 

