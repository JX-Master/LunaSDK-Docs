# Luna::RHI::OcclusionQueryMode::counting

```c++
counting = 1
```

Begins a counting occlusion query. In this query mode, the exact number of pixels that pass the depth/stencil test will be stored. 

