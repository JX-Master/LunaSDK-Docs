# Luna::Span::size_bytes

```c++
constexpr usize size_bytes() const
```

Gets the size of the span in bytes, which is `size() * sizeof(element_type)`. 



## Return value
Returns the size of the span in bytes. 

