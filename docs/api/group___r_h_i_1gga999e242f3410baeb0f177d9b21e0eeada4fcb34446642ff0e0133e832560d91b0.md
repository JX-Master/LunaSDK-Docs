# Luna::RHI::DescriptorSetLayoutFlag::variable_descriptors

```c++
variable_descriptors = 1
```

Enable variable-sized descriptors array for the last binding (the binding with the largest `binding_slot` value). 

