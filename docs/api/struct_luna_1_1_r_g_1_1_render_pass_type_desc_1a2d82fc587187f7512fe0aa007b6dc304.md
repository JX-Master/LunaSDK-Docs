# Luna::RG::RenderPassTypeDesc::userdata

```c++
ObjRef userdata
```

The optional userdata that will be passed to the compile callback function. The system keeps a strong reference to this object until module shutdown. 

