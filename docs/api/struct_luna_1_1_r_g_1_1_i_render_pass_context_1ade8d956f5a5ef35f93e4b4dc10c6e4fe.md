# Luna::RG::IRenderPassContext::get_command_buffer

```c++
virtual RHI::ICommandBuffer * get_command_buffer()=0
```

Gets the command buffer used to record commands for the render pass. 



## Return value
Returns the command buffer. 

