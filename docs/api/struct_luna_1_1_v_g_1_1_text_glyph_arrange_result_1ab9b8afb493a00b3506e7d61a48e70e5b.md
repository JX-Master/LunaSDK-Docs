# Luna::VG::TextGlyphArrangeResult::advance_length

```c++
f32 advance_length
```

The advance length of the glyph. This is not always equal to `bounding_rect.width`, because some characters may take more spaces than necessary for paddings. 

