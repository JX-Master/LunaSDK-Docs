# Luna::VG::IShapeDrawList::reset

```c++
virtual void reset()=0
```

Resets the draw list. The call clears all shapes recorded, but retains their memory and resources, so they can be reused for new shapes. 

