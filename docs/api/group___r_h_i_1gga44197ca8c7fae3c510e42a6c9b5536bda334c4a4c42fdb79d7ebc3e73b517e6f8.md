# Luna::RHI::BufferStateFlag::none

```c++
none = 0x00
```

This resource is not used by the pipeline. Resources with no state flag cannot be used by the pipeline, and must be transfered to one valid state before it can be used. 

