# Luna::RHI::ComputePassDesc::pipeline_statistics_query_heap

```c++
IQueryHeap* pipeline_statistics_query_heap
```

The pipeline statistics query heap that the query data will be written to if not `nullptr`. 

