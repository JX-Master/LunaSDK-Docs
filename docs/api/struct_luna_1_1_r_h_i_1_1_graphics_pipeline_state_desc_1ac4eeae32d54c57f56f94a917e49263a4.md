# Luna::RHI::GraphicsPipelineStateDesc::color_formats

```c++
Format color_formats[8]
```

The color attachment formats. Only [`0`, `num_color_attachments`) elements in this array will be used, other elements will be ignored. 

